# Shared Layer
Reusable UI components and global constants that can be accessed by any feature.

- `widgets/`: Atomic, reusable UI components like buttons, inputs, loading indicators.
- `constants/`: App-wide constants such as API URLs or text strings.
