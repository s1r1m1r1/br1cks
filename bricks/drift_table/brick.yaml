name: drift_table
description: A simple Drift table brick.
version: 0.1.0

vars:
  name:
    type: string
    description: The name of the table (singular, e.g. "task").
    prompt: Table name?
    default: task
